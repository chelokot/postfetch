{
  "name": "postfetch-template-cloudflare-worker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": { "deploy": "wrangler deploy", "dev": "wrangler dev", "typecheck": "tsc --noEmit" },
  "dependencies": { "@postfetch/core": "^0.2.0" }
}
