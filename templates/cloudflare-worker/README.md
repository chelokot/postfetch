# postfetch · Cloudflare Worker template

Edge deployment on top of [`@postfetch/core`](../../packages/core). Requires the
`nodejs_compat` flag (already set in `wrangler.toml`).

```bash
bun install
bunx wrangler dev
```
