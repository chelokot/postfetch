# postfetch · Azure Functions template

Azure Functions v4 (programming model) HTTP trigger on top of
[`@postfetch/core`](../../packages/core).

```bash
bun install
func start
```

Invoke with `?url=<post-url>` (or the URL as the request body).
